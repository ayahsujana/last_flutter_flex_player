enum InitializationEvent {
  initializing,
  initialized,
  uninitialized,
}

enum PlayerState {
  stopped,
  buffering,
  ready,
  playing,
  paused,
  ended,
}

enum ControlsStyle {
  defaultStyle,
  style1,
  style2,
  customStyle,
}

enum FileType {
  file,
  url,
  youtube,
}
